<%- include('./partials/header') %>
<nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/events"
      >Welcome, <%= currentUser.first_name %> !</a
    >
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto mb-2 mb-md-0">
        <li class="nav-item active">
          <a class="nav-link" href="/logout">Log out</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/">All Events</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/myEvents">My Events</a>
        </li>
      </ul>
    </div>
  </div>
</nav>
<div
  class="container-fluid d-flex flex-column overflow-auto h-100 justify-content-center"
>
  <!-- <a href="/logout" class="btn btn-info">Log out</a> -->
  <!-- Public Events  -->
  <% publicEvents.forEach(event => { %>
  <div class="card text-body my-1 bg-secondary bg-gradient">
    <div class="card-body overflow-auto">
      <h5 class="card-title"><%= event.name %></h5>
      <p class="card-text my-1">
        <%= event.start_time %> <%= event.start_date.toLocaleDateString('el-GR')
        %>
      </p>
      <p class="card-text my-1"><%= event.event_location %></p>
    </div>
  </div>
  <% }) %>
  <!-- My Events  -->
  <% myEvents.forEach(event => { %>
  <div class="card text-body my-1 bg-info bg-gradient">
    <div class="card-body overflow-auto">
      <h5 class="card-title"><%= event.name %></h5>
      <p class="card-text my-1">
        <%= event.start_time %> <%= event.start_date.toLocaleDateString('el-GR')
        %>
      </p>
      <p class="card-text my-1"><%= event.event_location %></p>
    </div>
  </div>
  <% }) %>
</div>

<%- include('./partials/eventsFooter') %>
